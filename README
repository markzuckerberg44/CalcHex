Andrés Hidalgo 21.795.550-5
Julian Honores 21.328.088-0

Local-Hosting paso a paso:
1. setear tu virtual enviroment:
  Los siguientes comandos en la terminal deberian setearlo:
    python -m venv djangoenv
    djangoenv\Scripts\activate
    pip install django
    pip install -r requirements.txt
2. Estar en la carpeta "sistCalc" -> cd sistCalc
3. Comenzar el local host -> python manage.py runserver
4. Click en el link del local host para ser redirigido a la pagina principal
